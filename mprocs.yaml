procs:
  backend:
    cmd: ["uv", "run", "uvicorn", "main:app", "--reload", "--host", "0.0.0.0", "--port", "8000"]
    cwd: services/backend

  frontend:
    cmd: ["pnpm", "dev"]
    cwd: services/frontend
